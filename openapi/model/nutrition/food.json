{
  "type": "object",
  "properties": {
    "accessLevel": {
      "type": "string",
      "enum": ["PUBLIC", "PRIVATE"],
      "description": "The access level of the food."
    },
    "brand": {
      "type": "string",
      "description": "The brand of the food."
    },
    "calories": {
      "type": "integer",
      "description": "The calories in the food."
    },
    "defaultServingSize": {
      "type": "number",
      "description": "The default serving size."
    },
    "defaultUnit": {
      "$ref": "#/components/schemas/FoodUnit"
    },
    "foodId": {
      "type": "integer",
      "description": "The ID of the food."
    },
    "isGeneric": {
      "type": "boolean",
      "description": "Whether the food is generic."
    },
    "locale": {
      "type": "string",
      "description": "The locale of the food."
    },
    "name": {
      "type": "string",
      "description": "The name of the food."
    },
    "servings": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/FoodServing"
      },
      "description": "List of servings."
    },
    "units": {
      "type": "array",
      "items": {
        "type": "integer"
      },
      "description": "List of unit IDs."
    }
  }
}
